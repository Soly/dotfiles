- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.current.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zshrc:
        force: true
        path: zshrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
